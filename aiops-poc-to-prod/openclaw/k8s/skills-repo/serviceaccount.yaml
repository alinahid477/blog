apiVersion: v1
kind: ServiceAccount
metadata:
  name: skills-repo
  namespace: openclaw
  labels:
    app.kubernetes.io/name: skills-repo
# This pod does not interact with the Kubernetes API.
automountServiceAccountToken: false
