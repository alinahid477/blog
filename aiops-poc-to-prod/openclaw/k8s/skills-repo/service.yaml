apiVersion: v1
kind: Service
metadata:
  name: skills-repo
  namespace: openclaw
  labels:
    app.kubernetes.io/name: skills-repo
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: skills-repo
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
