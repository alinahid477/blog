apiVersion: v1
kind: ServiceAccount
metadata:
  name: openclaw
  namespace: openclaw
  labels:
    app.kubernetes.io/name: openclaw
# The pod does not interact with the Kubernetes API — no SA token needed.
automountServiceAccountToken: false
